<h2>Classify Systems</h2>
<%= form_tag('/classification/' + @system_id, method: :put) do %>
    <% @topic.each do |t| %>
        <%= t.label %><br>
        <% @item.each do |i| %>
            <%= check_box_tag i.key, "1", checked = @classification.include?(i.id)%>
            <%= i.label %>
        <% end %>
    <% end %>
    <%= submit_tag %>
<% end %>